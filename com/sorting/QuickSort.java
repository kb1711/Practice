package com.sorting;

public class QuickSort {
 /*   int pivot=ar[0];
    int lowIndex=1;
    int highIndex=ar.length-1;
    while(lowIndex<=highIndex) {
      while(ar[lowIndex] < pivot){
            lowIndex++;
      }
       while(ar[highIndex] >= pivot){
            highIndex--;
        }
        if(lowIndex<highIndex){
        int temp=ar[lowIndex];
        ar[lowIndex]=ar[highIndex];
        ar[highIndex]=temp;
       // lowIndex++;
        //highIndex--;
        }
        printArray(ar); 
    }
   printArray(ar);     */
}
